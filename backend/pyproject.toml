[project]
name = "dinamikutb-backend"
version = "0.1.0"
description = "Backend de DinamikUTB"
requires-python = ">=3.12"
dependencies = [
    "fastapi",
    "uvicorn",
    "httpx2",
    "pytest",
    "sqlalchemy"
]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
